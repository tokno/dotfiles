#compdef wkd

_wkd() {
    _arguments \
        -c'[当日分以外の作業ディレクトリを削除します。]' \
        -h'[このコマンドの使い方を表示します。]' \
        -l'[作成済みの作業ディレクトリの一覧を表示します。]' \
        -s'[コマンド実行後のディレクトリ移動を行いません。]' \
        -t'[作成済みの作業ディレクトリ内をツリー表示します。]'
}

